{
  "name": "claude-ssh",
  "version": "1.0.0",
  "description": "Stdio MCP server that lets local Claude run commands on a VPS over SSH (no more copy-pasting logs).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "selftest": "node src/server.js --selftest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.5",
    "ssh2": "^1.17.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
